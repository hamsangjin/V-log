spring:
  application:
    name: velog

  datasource:
    url: jdbc:mysql://localhost:3306/velog
    username: sangjin
    password: sangjin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 2MB